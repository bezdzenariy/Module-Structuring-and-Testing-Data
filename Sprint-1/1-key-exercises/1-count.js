let count = 0;

count = count + 1;

// Line 1 is a variable declaration, creating the count variable with an initial value of 0
// Describe what line 3 is doing, in particular focus on what = is doing


// '=' is the assignment operator, no math
// algorithm reads from right to left

// count + 1 is evaluated first, which gives us 1
// then the result (1) is assigned back to count, so count now holds the value 1

console.log(count); // output: 1
